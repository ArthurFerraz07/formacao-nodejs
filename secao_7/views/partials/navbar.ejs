<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="/">Comum</a>
  <ul class="navbar-nav mr-auto">
    <li class="nav-item">
      <a class="nav-link" href="">Home</a>
    </li>
    <% if(categories){ %>
      <% categories.forEach(category => { %>
        <li class="nav-item">
          <a class="nav-link" href="/?categoryId=<%= category.id %>"><%= category.title %></a>
        </li>
      <% }); %>
    <% } %>
  </ul>
</nav>